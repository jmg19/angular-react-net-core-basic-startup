import { CheckUserExistDirective } from './check-user-exist.directive';

describe('CheckUserExistDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckUserExistDirective();
    expect(directive).toBeTruthy();
  });
});
